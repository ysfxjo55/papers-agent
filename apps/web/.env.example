# Use the same hostname the web app itself runs on (localhost, not 127.0.0.1) —
# the /auth session cookie is SameSite=Lax in dev, and localhost/127.0.0.1 count
# as different sites, so a mismatch here silently drops the session cookie.
NEXT_PUBLIC_API_URL=http://localhost:8000
